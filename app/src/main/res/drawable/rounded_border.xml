<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/darkblue" /> <!-- Background color -->
    <stroke
        android:width="4dp"
    android:color="@color/white" /> <!-- Border color (red) -->
    <corners
        android:radius="16dp" /> <!-- Radius for rounded corners -->
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp" /> <!-- Optional padding inside the border -->
</shape>